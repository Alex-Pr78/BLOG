import {
	authorize,
	logout,
	register,
	fetchUsers,
	fetchRoles,
	updateUserRole,
} from './operation';

export const server = {
	authorize,
	logout,
	register,
	fetchUsers,
	fetchRoles,
	updateUserRole,
};
